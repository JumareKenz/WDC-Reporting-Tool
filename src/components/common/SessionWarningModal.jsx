/**
 * Session Warning Modal
 *
 * Previously displayed an idle-timeout countdown. With the new silent
 * background token-refresh strategy, the warning is no longer needed.
 * The component is kept as a no-op so existing imports don't break.
 */
const SessionWarningModal = () => null;

export default SessionWarningModal;
