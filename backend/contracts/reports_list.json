{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "GET /api/reports — 200 response",
  "description": "Array of ReportListItem objects for the authenticated WDC secretary's ward. Each item is a lightweight summary; full detail is retrieved via GET /api/reports/{id}.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["id", "report_month", "status"],
    "properties": {
      "id": {
        "type": "integer"
      },
      "report_month": {
        "type": "string"
      },
      "status": {
        "type": "string"
      },
      "meetings_held": {
        "type": "integer"
      },
      "attendees_count": {
        "type": "integer"
      },
      "submitted_at": {
        "type": "string"
      },
      "has_voice_note": {
        "type": "boolean"
      }
    }
  }
}
