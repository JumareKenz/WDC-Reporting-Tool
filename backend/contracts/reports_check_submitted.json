{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "GET /api/reports/check-submitted — 200 response",
  "description": "Boolean flag indicating whether the current user has already submitted a report for the requested month, plus the report ID when present.",
  "type": "object",
  "required": ["submitted", "report_id"],
  "properties": {
    "submitted": {
      "type": "boolean"
    },
    "report_id": {
      "type": ["integer", "null"]
    }
  }
}
